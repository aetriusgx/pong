let BracketInstances = [];
class Bracket {
    constructor(x) {
        this.defaultY = y
        this.width = canvas.width * 0.025
        this.height = canvas.height * 0.25
        this.x = x
        this.y = canvas.height / 2 - this.height
        this.goal
        BracketInstances.push(this)
    }

    //Getters and Setters
    set goal(x) {
        this.goal = x
    }
    set y(y) {
        this.y = y
    }
    get x() {
        return this.x
    }
    get y() {
        return this.y
    }
    get x() {
        return this.x
    }
    get x() {
        return this.x
    }
}